apiVersion: v1
kind: Service
metadata:
  name: devops-app-service
spec:
  selector:
    app: devops-app
  type: NodePort
  ports:
    - protocol: TCP
      port: 8181       # Service port inside cluster
      targetPort: 8181 # Container port
      nodePort: 30082  # Port exposed on the node (host)
